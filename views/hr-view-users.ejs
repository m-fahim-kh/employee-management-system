<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <meta name="description" content="" />
  <meta name="author" content="" />
  <title>QodeHR</title>
  <base href="/" />
  <link href="https://cdn.jsdelivr.net/npm/simple-datatables@latest/dist/style.css" rel="stylesheet" />
  <link href="styles.css" rel="stylesheet" />
  <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/js/all.min.js" crossorigin="anonymous"></script>
</head>
<style>
  h1 {
    padding: 10px;
  }

  table {
    border-collapse: collapse;
    margin: 25px;
    font-size: 0.9em;
    font-family: sans-serif;
    min-width: 400px;
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.15);
  }

  table thead tr {
    margin-left: auto;
    margin-right: auto;
    background-color: #009879;
    color: #ffffff;
    text-align: left;
  }

  table th,
  table td {
    margin-left: auto;
    margin-right: auto;
    padding: 12px 15px;
  }
</style>

<body class="sb-nav-fixed">
  <%-include('partials/hr-header') %>
  <div style="background-color: rgba(255,255,255,0.13);" id="layoutSidenav">
    <div style="background-color: rgba(255,255,255,0.13);" id="layoutSidenav_nav">
      <%-include('partials/hr-side-nav') %>
    </div>

    <div id="layoutSidenav_content">

      <main>
        <div>
          <h1>All Employee Data</h1>
        </div>
        <div>
          <table>
            <thead>
              <tr>
                <th>ID</th>
                <th>Username</th>
                <th>Fisrt Name</th>
                <th>Last Name</th>
                <th>email</th>
                <th>Download Employee Data</th>
              </tr>
            </thead>
            <tbody>
              <% users.forEach((user)=>{%>
              <tr>
                <td><%=user.id%></td>
                <td><%=user.username%></td>
                <td><%=user.firstName%></td>
                <td><%=user.lastName%></td>
                <td><%=user.email%></td>
                <td><%=user.download%> <form action="/hr/user/<%=user.id%>"><button type="submit">Download <%=user.firstName%>'s Info</button></form>
                </td>
              </tr>
              <% });%>
            </tbody>
          </table>
        </div>
      </main>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>

</html>